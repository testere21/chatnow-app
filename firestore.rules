rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Tüm dokümanlar için geçici olarak tam erişim
    match /{document=**} {
      allow read, write: if true;
    }
  }
}